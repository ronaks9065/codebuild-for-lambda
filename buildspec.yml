version: 0.2

phases:
  build:
    commands:
    - echo "This is build phase... Zipping my lambda function"
    - zip -r deployment_package.zip .*/
  post_build:
    commands:
    - echo "Deploying to the Lambd"
artifacts:
  type: zip
  paths:
    - '*'
